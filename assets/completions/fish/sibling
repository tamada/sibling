complete -c sibling -s f -l format -d 'print the result in the specified format' -r -f -a "json\t''
csv\t''
list\t''
default\t''"
complete -c sibling -s s -l step -d 'specify the number of times to execute sibling. The negative count traverses in the opposite direction, and 0 means the current directory' -r
complete -c sibling -s t -l type -d 'specify the nexter type' -r -f -a "first\t''
last\t''
previous\t''
next\t''
random\t''
keep\t''"
complete -c sibling -s b -l base-path -d 'specify the parent directory of DIR (default: the parent directory of DIR)' -r -F
complete -c sibling -s A -l absolute -d 'print the directory name in the absolute path'
complete -c sibling -s p -l progress -d 'print the progress of traversing directories'
complete -c sibling -s a -l all -d 'Set the targets to all directories from the given list. By default, the sibling skips non-existent directories.'
complete -c sibling -s h -l help -d 'Print help'
